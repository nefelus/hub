var sqlTemplates = {
 hubQueue : 'SELECT hr.ID as ID, hr.COMMAND as COMMAND, hr.STATUS as STATUS, hr.SESSION_ID as SESSION_ID, hr.RUN_AS as RUN_AS, hr.MACHINE_ID as MACHINE_ID, hr.COMPANY_ID as COMPANY_ID, hr.USER_ID as USER_ID, hr.PROJECT_ID as PROJECT_ID, hr.TOOL_ID as TOOL_ID, hr.CPU_COUNT as CPU_COUNT, hr.COMMAND_FILE as COMMAND_FILE, hr.DATA_LOCATION as DATA_LOCATION, hr.DISPLAY as DISPLAY, hr.RESOLUTION as RESOLUTION, hr.SCHEDULED as SCHEDULED, hr.PRIORITY as PRIORITY, hr.QUEUE_INFO as QUEUE_INFO, u.LOGIN_NAME as LOGIN_NAME, l.ADDRESS as LICENSE_MANAGER from HUB_REQUEST as hr, USER_LOGIN as u, TOOL_APPS as t, LICENSE_MANAGER as l where hr.STATUS in ("PENDING", "SETUP", "RUNNING", "CLOSING") and hr.USER_ID = u.ID and hr.TOOL_ID = t.ID and t.LIC_SERVER = l.ID ORDER BY hr.PRIORITY DESC, hr.ID ASC',
 hubQueue_no_LIC_MANAGER : 'SELECT hr.ID as ID, hr.COMMAND as COMMAND, hr.STATUS as STATUS, hr.SESSION_ID as SESSION_ID, hr.RUN_AS as RUN_AS, hr.MACHINE_ID as MACHINE_ID, hr.COMPANY_ID as COMPANY_ID, hr.USER_ID as USER_ID, hr.PROJECT_ID as PROJECT_ID, hr.TOOL_ID as TOOL_ID, hr.CPU_COUNT as CPU_COUNT, hr.COMMAND_FILE as COMMAND_FILE, hr.DATA_LOCATION as DATA_LOCATION, hr.DISPLAY as DISPLAY, hr.RESOLUTION as RESOLUTION, hr.SCHEDULED as SCHEDULED, hr.PRIORITY as PRIORITY, hr.QUEUE_INFO as QUEUE_INFO, u.LOGIN_NAME as LOGIN_NAME from HUB_REQUEST as hr, USER_LOGIN as u where hr.STATUS in ("PENDING", "SETUP", "RUNNING", "CLOSING") and hr.USER_ID = u.ID ORDER BY hr.PRIORITY DESC, hr.ID ASC',
 tools      : 'SELECT t.ID as ID, t.NAME as NAME, v.NAME as VENDOR FROM TOOL_APPS as t, TOOL_VENDOR as v WHERE t.VENDOR_ID = v.ID ORDER BY VENDOR, NAME ASC',
 machineTypes : 'SELECT ID, NAME, DESCRIPTION, CLOUD_ID, CPU_CNT, MEMORY_GBS FROM MACHINE ORDER BY CLOUD_ID, NAME',
 notificationInfo : "SELECT us.ID as ID, up.BLOCK as blockname, up.NAME as projectname, ul.FIRST_NAME as firstname, concat(concat(ul.FIRST_NAME, ' '), ul.LAST_NAME) as fullname, ul.EMAIL as email, hr.STARTED as started, hr.COMPL_TARGET as completed, hr.STATUS as status, ta.NAME as toolname FROM TOOL_APPS as ta, HUB_REQUEST as hr, USER_SESSION as us, COMPANY_PROJECTS as up, USER_LOGIN as ul WHERE us.TASK_ID = ? and ul.ID = ? and up.ID = ? and us.USER_ID=ul.ID and hr.ID = ? and ta.ID = ?",
 userlist : "SELECT a.ID, a.FIRST_NAME, a.LAST_NAME, a.COMPANY_ID, b.NAME as COMPANY_NAME, a.EMAIL, a.ADMIN, a.CREATED from USER_LOGIN as a , TOOL_VENDOR as b where a.COMPANY_ID = b.ID",
 getuserbyid : "SELECT a.ID, a.FIRST_NAME, a.LAST_NAME, a.COMPANY_ID, b.NAME as COMPANY_NAME, a.EMAIL, a.ADMIN, a.CREATED from USER_LOGIN as a , TOOL_VENDOR as b where a.COMPANY_ID = b.ID and a.ID = ?",
 getusersessions : "SELECT ID, SESSION_ID, COMMAND, STATUS, CREATED, STARTED, COMPLETED  from HUB_REQUEST where USER_ID = ? and COMMAND like 'EXEC%'",
 getsessionbyid : "SELECT ID, SESSION_ID, COMMAND, STATUS, CREATED, STARTED, COMPLETED  from HUB_REQUEST where SESSION_ID = ? and COMMAND like 'EXEC%'",
 getqueuequotas_inactive : 'SELECT * FROM QUEUE_QUOTA WHERE ACTIVE <> "N" ORDER by COMPANY_ID,USER_ID,PROJECT_ID, RESOURCE_TYPE, RESOURCE_ID',
 getqueuequotas : 'SELECT * FROM QUEUE_QUOTA WHERE (ACTIVE is NULL or ACTIVE <> "N") ORDER by COMPANY_ID,USER_ID,PROJECT_ID, RESOURCE_TYPE, RESOURCE_ID',
 getprojectusers : 'SELECT * FROM QUEUE_QUOTA WHERE (ACTIVE is NULL or ACTIVE <> "N") and RESOURCE_TYPE="USERS" and MAX_ALLOWED > 0 ORDER by COMPANY_ID,USER_ID,PROJECT_ID, RESOURCE_ID',
 machines : 'SELECT * FROM MACHINE WHERE (ACTIVE is NULL or ACTIVE<>"N") ORDER BY CLOUD_ID, NAME',
 shares : 'SELECT m.ID as ID, m.RESOURCE_TYPE as RESOURCE_TYPE, m.UUID as UUID, f.CLOUD_ID as CLOUD_ID, concat(f.LOCAL_IPV4,":",v.MOUNT_POINT,"/",m.DIR) as LOCATION, m.MOUNT_POINT as MOUNT_POINT, m.MOUNT_PARAMS as MOUNT_PARAMS, m.FS_TYPE as FS_TYPE FROM MOUNT_POINTS as m, FILE_MANAGER as f, VOLUMES as v WHERE (m.ACTIVE is NULL or m.ACTIVE <> "N") and v.ID = m.VOLUME_ID and v.FILEMANAGER_ID = f.ID and m.RESOURCE_TYPE in ("SHARED_DATA", "USER_DATA", "IP_DATA", "IP_DATA_LIB", "TOOLS_DATA", "IP_DOCS", "TOOL_DOCS")',
 toolapps : 'SELECT * FROM TOOL_APPS WHERE (ACTIVE is NULL or ACTIVE<>"N")',
 clouds : 'SELECT * FROM CLOUD WHERE (ACTIVE is NULL or ACTIVE<>"N") ORDER BY CLOUD_TYPE;',
 dict : 'SELECT * FROM DICTIONARY WHERE (ACTIVE is NULL or ACTIVE<>"N") ORDER BY DICT,SEQNO;',
 images : 'SELECT * FROM IMAGES WHERE (ACTIVE is NULL or ACTIVE<>"N") ORDER BY CLOUD_ID, TOOL_ID, VIRTUALIZATION;',
 secgroups : 'SELECT sg.ID as SEC_GROUP_ID, sg.COMPANY_ID as COMPANY_ID, sg.PROJECT_ID as PROJECT_ID, sgr.ID as ID, sgr.DIRECTION as DIRECTION, sgr.INTERFACE as INTERFACE, sgr.PROTOCOL as PROTOCOL, sgr.ADDRESS as ADDRESS, sgr.PORTS as PORTS FROM SEC_GROUPS as sg, SEC_GROUP_RULES as sgr where (sgr.ACTIVE is NULL or sgr.ACTIVE <> "N") and (sg.ACTIVE is NULL or sg.ACTIVE <> "N") and sg.ID=sgr.SEC_GROUP_ID ORDER BY SEC_GROUP_ID, ID;',
 lastupdated : 'SELECT * FROM LASTUPDATED WHERE TABNAME IN ("SEC_GROUP_RULES", "QUEUE_QUOTA", "MOUNT_POINTS", "VOLUMES", "FILE_MANAGER", "MACHINE", "IMAGES", "CLOUD", "LICENSE_MANAGER", "TOOL_APPS");'
};

exports.sqlTemplates = sqlTemplates;
